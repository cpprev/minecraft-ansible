all:
    hosts:
        mc_server:
            ansible_host: 35.180.251.1

            # Remote host password (incase ssh password auth)
            #ansible_password = yourpassword

            # Private key file path (incase ssh private key auth)
            ansible_ssh_private_key_file: "mc-serv.pem"
