all:
    hosts:
        mc_server:
            ansible_host: 35.180.251.1

            # Remote host password (not needed depending on SSH config)
            #ansible_password = toto123lol

            # private key (needed to ssh to aws box)
            ansible_ssh_private_key_file: "mc-serv.pem"
